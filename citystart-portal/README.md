# CityStart Portal

静态 HTML、Bootstrap 和原生 JavaScript Portal，只通过 API Gateway 通信。

```powershell
python -m http.server 3000
```

浏览器打开 `http://localhost:3000`。Gateway 地址默认是 `http://localhost:8000`，可在 `config.js` 修改。

